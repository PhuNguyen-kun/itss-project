const express = require("express");
const router = express.Router();
const ingredientsController = require("../../controllers/user/ingredients.controller");

// router.get("/:id", ingredientsController.getIngredientsById);